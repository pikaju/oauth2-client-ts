{
  "name": "example",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "yarn build:tsc",
    "build:tsc": "tsc --declaration",
    "format": "eslint --fix --fix-type layout .",
    "lint": "eslint --no-fix ."
  },
  "dependencies": {
    "oauth2-client-ts": "file:../"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.4"
  }
}
